<USER_REQUEST>
/brainstorming resolvas estes erros apresentados

Vou fazer uma leitura mais focada nas views de navegaÃ§Ã£o e fluxos.Agora tenho tudo que preciso. Aqui estÃ¡ a anÃ¡lise focada e separada:

---

## Erros de NavegaÃ§Ã£o

**N1 â€” `cadastrar-reserva` e `checkout-plano` invisÃ­veis para a Navbar autenticada**
A Navbar sÃ³ exibe o menu interno (`Painel Principal`, `ConfiguraÃ§Ãµes`, `Sair`) nas rotas `['hub', 'sales-stats', 'dashboard', 'assinar', 'configuracoes']`. Nas rotas `cadastrar-reserva` e `checkout-plano`, um usuÃ¡rio logado vÃª o menu pÃºblico da homepage (com "Assinar" e "Acesso Lojista"), como se tivesse deslogado.

**N2 â€” BotÃ£o "Voltar" do `CadastroReservaClienteView` no Step 1 leva para `home` em vez de `dashboard`**
```tsx
// linha 2929
if (step === 1) { navigateTo('home'); }
```
O fluxo correto: o vendedor chega ao cadastro vindo do `dashboard` â†’ ao voltar, deveria ir para `dashboard`. A home nÃ£o faz sentido para um usuÃ¡rio logado no meio de um fluxo interno.

**N3 â€” ApÃ³s publicar uma reserva (`handlePublish`), o usuÃ¡rio Ã© levado para `home`**
```tsx
// linha 1430
navigateTo('home');
```
ApÃ³s confirmar e publicar uma proposta, o vendedor vai parar na homepage pÃºblica. O destino correto seria `dashboard` ou `sales-stats`, para ele ver o link recÃ©m-criado na lista.

**N4 â€” `PreviewView` em modo visualizaÃ§Ã£o (nÃ£o prÃ©-publicaÃ§Ã£o) tem botÃ£o "Voltar ao Sistema" que vai para `dashboard`, mas nÃ£o hÃ¡ como chegar no preview a partir do `sales-stats`**
No `SalesStatsView` os botÃµes "Desktop" e "Mobile Sim" (linhas 1346â€“1362) chamam `navigateTo('preview')`, mas a lÃ³gica de retorno do preview usa `isPrePublish` para decidir o destino. Quando o usuÃ¡rio vem do `sales-stats`, `isPrePublish` Ã© `false` e o botÃ£o "Voltar ao Sistema" vai para `dashboard` â€” nÃ£o de volta para `sales-stats` de onde ele veio.

**N5 â€” `AssinaturaEmpresaView` tem 4 steps no progress bar mas sÃ³ renderiza atÃ© o step 3**
```tsx
// linha 3066 â€” barra mostra 4 step
<truncated 4088 bytes>
hub')` funciona de qualquer lugar, inclusive direto da URL ou de um botÃ£o na home.

**F3 â€” Nenhuma validaÃ§Ã£o de CNPJ no `AssinaturaEmpresaView`**
O campo CNPJ tem mÃ¡scara de formataÃ§Ã£o, mas nÃ£o valida se o CNPJ Ã© matematicamente vÃ¡lido. Um CNPJ como `00.000.000/0000-00` passa sem erro.

**F4 â€” NÃ£o hÃ¡ confirmaÃ§Ã£o antes de "Limpar Tudo" no `DashboardView`**
```tsx
// linha 1291
onClick={() => { setRecentReservations([]); showToast(...) }}
```
Um clique acidental apaga todas as reservas sem nenhum modal de confirmaÃ§Ã£o ou possibilidade de desfazer.

**F5 â€” NÃ£o existe tela de detalhe de reserva individual**
No `DashboardView` e `SalesStatsView`, cada card tem botÃµes de "Desktop/Mobile" para ver o preview, mas nÃ£o hÃ¡ uma tela de gestÃ£o da reserva especÃ­fica (status, histÃ³rico de visualizaÃ§Ãµes, editar sinal, cancelar reserva).

**F6 â€” O `MobileClientView` nÃ£o tem botÃ£o de voltar funcional ao contexto correto**
Por analogia com o `PreviewView` (N4), o `MobileClientView` provavelmente sofre do mesmo problema de destino de retorno, mas o fluxo de retorno nÃ£o foi implementado explicitamente para diferenciar se o usuÃ¡rio veio do `sales-stats` ou do `dashboard`.

**F7 â€” NÃ£o hÃ¡ feedback visual de carregamento no Step 1 do cadastro ao trocar de marca/modelo**
O `isFipeLoading` existe e Ã© usado para exibir um overlay geral, mas a seleÃ§Ã£o de modelos/anos ao mudar a marca nÃ£o tem loading state individual nos selects â€” eles ficam habilitados instantaneamente mesmo antes dos dados carregarem.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-05-31T19:10:46-03:00.

The user has mentioned some items in the form @[ITEM]. Here is extra information about the items that were mentioned by the user, in the order that they appear:

/brainstorming is a [Slash Command]:
<SKILL>The user requested you read and use the "brainstorming" skill. The path to the skill file is:
C:\Users\Wollace\.gemini\config\skills\brainstorming\SKILL.md</SKILL>
</ADDITIONAL_METADATA>
